/*
作者:
    liuziming

创建时间：
    2014.08.28(星期四) 

意图：
  设置指定角色的显示/隐藏

公共接口：

*/
/*
class Movie_VisiblePlayer extends Movie_Elem {
	visible
	playerId
	public initObj(...args: any[]): void {
		this.visible = args[0].visible || false
		this.playerId = args[0].var
		//TLog.Debug("init",this.visible,args[1].visible)
	}

	onBegin() {
		let player = MovieSystem.getInstance().getPlayer(this.playerId)
		if (player) {
			player.setVisible(this.visible)
			//TLog.Debug("Movie_VisiblePlayer.onBegi",this.visible)
		}

		this.finish()
	}

	onTick(delay) {

	}

	destory() {

	}

	onFinish() {

	}
}*/